# agent-skills

Starter repository for future agent skills.

## Current state

- No skills are currently published.
- `skills/` is intentionally kept as a starter directory for future additions.
- `.claude-plugin/marketplace.json` is intentionally initialized with an empty plugin list.

## Starter layout

```text
.
├── .claude-plugin/marketplace.json
├── .github/workflows/
├── skills/
│   └── .gitkeep
├── AGENTS.md
└── README.md
```

## Validation

```bash
claude plugin validate .
```
